{% extends 'partials/base.html.twig' %}

{% block content %}
<div class="about-section limiter">
    
    <section class="image-vega">
    	<img class="image-support" src="{{ url(page.header.bgImage|keys|first) }}" alt="">
    </section>

    <section class="info-vega">
        <h1>{{ header.title }}</h1>
        <p>{{ header.description }}</p>
    </section>

</div>
{% endblock %}