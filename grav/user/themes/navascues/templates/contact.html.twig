{% extends 'partials/base.html.twig' %}

{% block content %}
<div class="about">
	{{ page.content}}
    <section>
    {% if grav.language.getActive == 'es' %}
        <h1 class="contact-title">Contáctenos</h1>
    {% endif %}
    {% if grav.language.getActive == 'en' %}
        <h1 class="contact-title">Contact Us</h1>
    {% endif %}
    </section>
    <div class="info">
        <img class="image-contact" src="{{ url(page.header.contactImage|keys|first) }}" alt="">
        <div class="info-contact">
            <h2>{{ header.contactTitle }}</h2>
            <p>{{ header.mail }}</p>
        </div>
    </div>

</div>
{% endblock %}